<div class="container">
    <img src="{{ asset($imagePath) }}" alt="{{ $filename }}" class="img-fluid">
    <a href="{{ route('dashboard') }}" class="btn btn-primary mt-3">Back to Dashboard</a>
</div>